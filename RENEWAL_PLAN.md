# Dr.Clarity 프로젝트 리뉴얼 및 확장 계획서

> **상태: 완료 (2026-08-16).** 이 문서는 Vanilla JS에서 Next.js App Router로 전환한 당시의 계획 기록입니다. 현재 제품 범위와 시스템 구조는 [`prd.md`](./prd.md)와 [`sdd.md`](./sdd.md)를 따릅니다.
>
> 이 문서는 **왜/무엇을** 다룹니다. **어떻게** 구현하는지는 [`IMPLEMENTATION_SPEC.md`](./IMPLEMENTATION_SPEC.md)에 있습니다.
> 구현을 담당하는 코드 에이전트는 이 문서로 맥락을 잡은 뒤 구현 명세서를 따르십시오.

---

## 1. 개요 및 리뉴얼 목표

- **목적**: 기존 Vanilla JS 단일 페이지 애플리케이션을 AI(코드 에이전트) 기반의 바이브 코딩(Vibe Coding)에 최적화된 확장 가능한 구조로 전면 개편합니다.
- **핵심 목표**:
  - 컴포넌트화 및 파일 기반 라우팅 도입.
  - 향후 AI가 새로운 주제(컴퓨터 사이언스, 수학, AI 기초 등)를 생성할 때 **기존 코드의 수정 없이** 통합될 수 있는 플러그인 스타일의 구조 마련.
  - 애니메이션 및 캔버스(Canvas) 기반 인터랙티브 콘텐츠의 상태 관리를 안정적으로 수행.

## 2. 현재 상태 진단 (as-is)

| 항목 | 현황 |
|---|---|
| 스택 | Vite 7 + Vanilla JS, 빌드 도구 외 의존성 없음 |
| 구조 | `index.html` 하나에 3개 주제를 모두 담고 CSS `display:none` 토글로 전환 |
| 로직 | `main.js` 795줄, 전역 스코프, 모듈 분리 없음 |
| 스타일 | `style.css` 621줄 전역, 디자인 토큰 6개 외 대부분 하드코딩 색상 |
| 잔재 | `src/` 디렉터리 전체가 Vite 스캐폴드 미사용 코드 |

**구조적 한계** — 새 주제를 하나 추가하려면 `index.html`, `main.js`, `style.css` 세 파일을 모두 수정해야 합니다. 전역 스코프 때문에 주제 간 이름 충돌 위험이 있고, 애니메이션 정리(cleanup) 지점이 없어 화면 전환 시 `requestAnimationFrame` 루프가 계속 살아 있습니다. 이는 "AI가 기존 코드 수정 없이 콘텐츠를 추가한다"는 목표와 정면으로 충돌합니다.

식별된 개별 결함 20건의 목록과 처리 방침은 구현 명세서 [§9 레거시 결함 대장](./IMPLEMENTATION_SPEC.md#9-레거시-결함-대장)에 정리되어 있습니다.

## 3. 확정된 의사결정

아래 항목은 **결정 완료**이며, 구현 중 재논의 대상이 아닙니다. 변경이 필요하면 작업을 중단하고 사용자에게 확인하십시오.

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| D1 | 배포 대상 | **Vercel** (SSR/ISR 사용 가능) | `next/image` 최적화, 동적 메타데이터 활용 가능. `output: 'export'` 제약 없음 |
| D2 | 코드 배치 | **현재 워크트리 in-place 교체** | `landfill/rudd` 워크트리이며 `main` 브랜치에 레거시가 온전히 보존됨. 별도 백업 디렉터리 불필요 |
| D3 | 언어 | **TypeScript 확정** | 컴포넌트 props 계약을 타입으로 강제해 바이브 코딩 오류를 줄임 |
| D4 | 콘텐츠 등록 | **파일 규약 기반 자동 수집** | 각 주제 디렉터리의 `meta.ts`를 빌드 시 스캔. 중앙 registry 파일 수동 편집 불필요 |
| D5 | 꿀통 퍼즐 | **풀이 단계 UI 신규 제작** | 현재 문제만 있고 풀이가 없음. 이진법 인코딩 인터랙티브 컴포넌트로 구현 |
| D6 | TopicLayout 범위 | **진행 상태바 / 이전·다음 퍼즐 버튼 제외** | 이번 리뉴얼은 순수 이식 + 구조 안정화에 집중. 구조가 자리잡은 뒤 추가 |
| D7 | 배경 애니메이션 | **제거** | `setInterval` 200ms로 DOM 노드를 무제한 생성하는 구조. 성능·접근성 모두 불리 |

## 4. 기술 스택

- **Framework**: Next.js (App Router) — 설치 시점 최신 버전 사용
- **Language**: TypeScript (D3)
- **Styling**: Vanilla CSS + CSS Modules (컴포넌트 단위 캡슐화), 전역 디자인 토큰은 `globals.css`
- **Font**: `next/font/google`의 Outfit (weight 300/500/700) — 레거시의 `<link>` 방식 대체
- **Test**: Vitest (순수 로직 한정 — 기하 계산, 이진 인코딩, 레지스트리 생성기)

## 5. 라우팅 및 메뉴 구조

| 경로 | 화면 | 비고 |
|---|---|---|
| `/` | 홈 — 카테고리 소개 + 퍼즐 카드 대시보드 | 레지스트리에서 자동 생성 |
| `/math` | 수학 퍼즐 카테고리 인덱스 | 레지스트리에서 자동 생성 |
| `/math/honey-pots` | 25개의 꿀통과 5마리 개미 | 이식 + 풀이 UI 신규 (D5) |
| `/math/geometry-area` | 기하학 퍼즐 (빨간 영역의 넓이) | 이식 |
| `/cs` | 컴퓨터 사이언스 기초 카테고리 인덱스 | 레지스트리에서 자동 생성 |
| `/cs/floating-point` | 부동소수점 오류 | 이식 |
| `/ai` | AI 기초 카테고리 인덱스 | 콘텐츠 없는 빈 상태(empty state) |

카테고리 인덱스와 GNB는 개별 주제를 하드코딩하지 않고 자동 수집된 레지스트리를 렌더링합니다. 즉 **새 주제 디렉터리 하나를 추가하면 홈·GNB·카테고리 인덱스·사이트맵에 자동 반영**됩니다.

## 6. 재사용 가능한 컴포넌트 아키텍처

AI 에이전트가 새 퍼즐 화면을 밑바닥부터 짜지 않고 레고 블록처럼 조립할 수 있도록 기반 컴포넌트를 설계합니다. 전체 props 계약은 구현 명세서 [§6](./IMPLEMENTATION_SPEC.md#6-공통-컴포넌트-계약)을 참조하십시오.

| 컴포넌트 | 역할 |
|---|---|
| `TopicLayout` | 학습 페이지 공통 골격 (제목 / 부제 / 힌트 / 콘텐츠 슬롯) |
| `InteractiveCanvas` | Canvas 래퍼 — DPI 대응, 리사이즈, 웹폰트 로드 대기, cleanup 내장 |
| `AnimationCard` | 애니메이션 콘텐츠용 시각적 카드 (컨트롤 영역 + 상태 텍스트 슬롯) |
| `ExplanationBox` | 개념 설명 하이라이트 박스 / 아코디언 |
| `SolutionStepper` | 단계별 풀이 진행 UI — 기하 퍼즐과 꿀통 퍼즐이 공유 |
| `TopicCard` | 대시보드·카테고리 인덱스의 퍼즐 카드 |

## 7. 마이그레이션 단계 (Action Plan)

각 단계는 독립 커밋 단위이며, 상세 작업 지시는 구현 명세서 [§10 작업 순서](./IMPLEMENTATION_SPEC.md#10-작업-순서)에 있습니다.

| Step | 내용 | 완료 기준 |
|---|---|---|
| 1 | 기반 환경 구축 — Next.js 설치, 디자인 토큰 이식, Outfit 폰트 설정, Vite 잔재 제거 | `next build` 성공, 빈 홈 화면 렌더 |
| 2 | 콘텐츠 레지스트리 — 타입 정의 + 자동 수집 스크립트 | 더미 주제 1개로 홈·GNB 자동 반영 확인 |
| 3 | 공통 컴포넌트 6종 구현 | 각 컴포넌트가 타입 체크 통과 및 홈에서 사용됨 |
| 4 | `/cs/floating-point` 이식 | 피자 애니메이션·계산기 연출 동작, cleanup 검증 |
| 5 | `/math/geometry-area` 이식 | 6단계 풀이 전부 동작, DPI 대응, rAF 누수 없음 |
| 6 | `/math/honey-pots` 이식 + 풀이 UI 신규 | 6단계 풀이 + 이진 인코딩 인터랙션 동작 |
| 7 | 홈 대시보드 / 카테고리 인덱스 / `/ai` 빈 상태 | 전 경로 렌더, 사이트맵 생성 |
| 8 | 바이브 코딩 검증 — 신규 주제 추가 리허설 | [§11 수용 기준](./IMPLEMENTATION_SPEC.md#11-수용-기준) 전 항목 통과 |

## 8. 이번 리뉴얼의 비범위 (Out of Scope)

아래는 **의도적으로 제외**합니다. 요청 없이 착수하지 마십시오.

- 진행 상태바, 이전/다음 퍼즐 네비게이션 (D6)
- 다크 모드 / 테마 전환
- 다국어(i18n) — 한국어 단일 언어
- 사용자 계정, 진도 저장, 백엔드/DB
- 검색, 태그 필터링
- 신규 학습 주제 콘텐츠 (`/ai` 하위는 빈 상태만 제공)
- 애널리틱스, A/B 테스트

## 9. 성공의 정의

리뉴얼이 성공했다면, **코드 에이전트가 `src/app/(topics)/<카테고리>/<주제>/` 디렉터리 하나에 `page.tsx`와 `meta.ts`만 만들어도** 홈 대시보드·GNB·카테고리 인덱스·사이트맵에 자동 등록되고, 기존 파일은 단 한 줄도 수정되지 않아야 합니다. 이것이 [§11 수용 기준](./IMPLEMENTATION_SPEC.md#11-수용-기준)의 최종 관문입니다.
