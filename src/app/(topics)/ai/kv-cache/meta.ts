import { TopicMeta } from '@/content/types';

const meta: TopicMeta = {
  title: 'KV 캐시 — 앞에서 한 계산을 다시 쓴다면?',
  summary: '캐시를 켜고 끄며 K·V를 다시 만드는 작업과 저장할 메모를 비교합니다.',
  order: 5,
  difficulty: 2, // K·V 재사용과 저장 공간을 비교하는 핵심 체험 기준.
  tags: ['AI', '추론', '캐시', '복잡도'],
  series: 'llm-inference',
  seriesOrder: 5
};

export default meta;
