export const title = '앞에서 한 계산, 또 해야 할까?';

export const choices = [{"id": "hundred", "label": "이전 토큰의 K·V를 매번 다시 만든다"}, {"id": "same", "label": "이전 K·V는 다시 쓰고, 새 토큰의 것만 만든다"}, {"id": "random", "label": "앞의 정보를 모두 버린다"}];

K·V는 앞의 토큰에서 만들어 둔 계산 결과입니다. **KV 캐시를 켜면** 다음 단계에서 무엇이 달라질까요?
