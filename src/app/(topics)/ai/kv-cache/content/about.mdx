export const title = '이 격자가 생략한 것';

K·V는 이전 토큰에서 만든 두 종류의 숫자 묶음입니다. 기본적인 KV 캐시는 이 값을 저장해 재사용합니다.

격자는 K·V를 새로 만드는 작업만 셉니다. 캐시를 켜도 과거 정보에 주목하는 계산은 필요하므로, 칸 수의 비율이 실제 속도 향상률은 아닙니다. 긴 문맥에서는 캐시를 읽는 양과 메모리 부담도 커질 수 있습니다.

<a href="https://huggingface.co/docs/transformers/cache_explanation">캐시 원리 — Hugging Face</a>
