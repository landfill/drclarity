export const title = '먼저 생각해 보기';

export const choices = [
  { id: 'window', label: '한 번의 호출이 그만큼 큰 문맥을 봤다 — 333만이 한 번에 들어갔다' },
  { id: 'model', label: '더 비싼 모델로 갔다 — 같은 양이라도 단가가 높았다' },
  { id: 'calls', label: '한 번이 아니라 여러 번 불렀다 — 작은 문맥이 그만큼 다시 실렸다' },
];

한 사람이 하루에 보낸 요청 다섯 건입니다. 넷은 24만에서 58만 토큰 사이인데
한 건만 <strong>333만</strong>이고, 그 한 건의 비용이 나머지 넷을 합친 것보다 큽니다.

대시보드가 알려주는 것은 이 표가 전부입니다. **어떤 프롬프트였는지는 적혀 있지 않습니다.**
