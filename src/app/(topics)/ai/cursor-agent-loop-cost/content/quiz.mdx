export const title = '먼저 생각해 보기';

export const choices = [
  { id: 'scope', label: '앞에 「장바구니 폴더에서」 를 붙인다 — 어디를 볼지 짚어 준다' },
  { id: 'stop', label: '뒤에 「없으면 없다고만 답해. 더 찾지 마.」 를 붙인다 — 언제 그만둘지 정해 준다' },
  { id: 'both', label: '둘 다 위 한 줄보다 싸다 — 어느 쪽을 붙여도 줄어든다' },
];

한 사람이 하루에 보낸 요청 다섯 건입니다. 넷은 24만에서 58만 토큰 사이인데
한 건만 <strong>333만</strong>이고, 그 한 건의 비용이 나머지 넷을 합친 것보다 큽니다.
