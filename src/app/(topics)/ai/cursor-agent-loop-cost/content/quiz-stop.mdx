이쪽은 반대입니다. `없으면 없다고만 답해. 더 찾지 마.` 를 붙이면 333만이 **58만**으로
떨어집니다. 호출이 33번에서 10번이 됩니다.

값을 한 것은 이쪽이 맞습니다. 다만 이 문제가 물은 것은 **더 비싼 쪽**이었습니다.

남은 하나 — 범위를 짚는 쪽이 **402만**입니다. 짚어 준 범위는 **시작할 칸**을 정할 뿐,
거기서 못 찾으면 그 위로 올라갑니다. 좁은 칸을 더 뒤진 값만 얹힙니다.

아래에서 그 과정을 봅니다.
