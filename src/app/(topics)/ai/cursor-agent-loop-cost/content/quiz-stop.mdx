맞습니다. `없으면 없다고만 답해. 더 찾지 마.` 를 붙이면 333만이 **58만**으로 떨어집니다.
호출이 33번에서 10번이 됩니다.

`장바구니 폴더에서` 쪽도 줄기는 합니다 — **96만**. 다만 짚어 준 범위에 없으면 에이전트는
**한 칸 넓혀 봅니다.** 그 한 칸이 남아 있어서 덜 줄었습니다.

<strong>둘은 서로를 대신하지 못합니다.</strong> 같이 적으면 24만입니다.
아래에서 손으로 고쳐 봅니다.
