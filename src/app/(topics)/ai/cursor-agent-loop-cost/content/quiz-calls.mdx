`Cache Read` 가 395만입니다. 캐시 읽기는 **이번 호출에 들어간 입력 가운데 다시 계산하지
않고 가져온 몫**이므로, 이 수는 같은 문맥이 여러 번 다시 실렸다는 기록입니다.

여기까지가 대시보드가 답해 주는 데까지입니다. 그런데 이 글이 묻는 것은 그다음입니다 —
<strong>왜 그렇게 여러 번이었나.</strong>

대시보드에 적혀 있지 않은 것이 하나 있습니다. **그 요청의 프롬프트가 무엇이었나.**
아래에서 그것을 손으로 고쳐 봅니다.
