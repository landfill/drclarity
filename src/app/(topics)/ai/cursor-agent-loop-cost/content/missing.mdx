export const title = '없는 것을 찾을 때, 그리고 너무 좁게 짚을 때';

<strong>찾는 것이 있다</strong>를 켜고 `어디서`(폴더) · `언제 그만둘지` ·
`무엇을 돌려줄지` 를 다 켜 보세요. $0.183 에서 $0.073 — **2.5배**입니다.
끄고 같은 일을 하면 $0.793 에서 $0.073 — **11배**입니다.

<strong>끝값이 같습니다.</strong> 잘 적으면 있으나 없으나 같은 값이 나옵니다. 더 적은 것
없이 두면 **있느냐 없느냐만 달라도 4.3배**입니다.

멈출 이유가 생기느냐의 차이입니다. 찾으면 **찾았다는 사실이 멈출 이유**가 됩니다.
못 찾으면 그런 것이 생기지 않습니다. 그래서 프롬프트를 고치는 보람이 있는 것은
**없을 때**이고, 없을지 있을지는 대개 미리 알 수 없습니다.

<strong>여기에 반대쪽 함정이 있습니다.</strong>

`어디서` 를 파일까지 좁히고 `언제 그만둘지` 를 켠 채로 <strong>찾는 것이 있다</strong>를
켜 보세요. 배지가 **있는데 못 찾음**으로 바뀝니다. 2호출, $0.028 — **가장 싼 조합인데
답이 틀렸습니다.**

짚어 준 파일에 없었고, 멈추라고 했으니 그 밖은 보지 않았습니다. 어느 파일인지 **알 때**
짚는 것입니다. 확신이 없으면 한 칸 넓게 짚는 편이 낫습니다.
