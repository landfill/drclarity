줄어드는 것은 맞습니다. `장바구니 폴더에서` 를 붙이면 333만이 **96만**이 됩니다.
다만 더 크게 줄이는 쪽은 반대편입니다 — `없으면 없다고만 답해. 더 찾지 마.` 가
**58만**입니다.

짚어 준 범위는 탐색을 좁은 칸으로 데려다 놓습니다. 그런데 거기 없으면 에이전트는
**한 칸 넓혀 봅니다** — 그 폴더에 없으니 프로젝트 전체를. 그 한 칸을 없애는 것이
중단 조건입니다.

<strong>둘은 서로를 대신하지 못합니다.</strong> 같이 적으면 24만입니다.
아래에서 손으로 고쳐 봅니다.
