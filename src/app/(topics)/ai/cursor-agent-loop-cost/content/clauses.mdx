export const title = '범위만 좁히면 오히려 비싸진다';

체크박스 조합을 값 순으로 늘어놓으면 이렇습니다. 찾는 것이 **없을 때**입니다.

- 아무것도 안 적음 — 33호출 · **$0.793**
- `무엇을` — 17호출 · **$0.354**
- `무엇을` + `어디서`(폴더) — 20호출 · **$0.418**
- `무엇을` + `어디서`(파일·함수) — 21호출 · **$0.438**
- `무엇을` + `언제 그만둘지` — 5호출 · **$0.084**
- `무엇을` + `어디서`(폴더) + `언제 그만둘지` — 3호출 · **$0.045**
- `무엇을` + `어디서`(파일·함수) + `언제 그만둘지` — 1호출 · **$0.013**

세 번째와 네 번째 줄을 보세요. `src/config 안에서` 라고 짚어 주었는데 **두 번째 줄보다
비쌉니다.** 더 좁게 짚으면 더 비쌉니다.

사다리를 보면 이유가 보입니다. **도달한 칸이 그대로 `작업 폴더 밖`입니다.** 좁게 시작한
만큼 좁은 칸의 탐색값을 더 냈고, 못 찾은 뒤에는 어차피 위로 올라갔습니다. 아낀 것은
없고 더 낸 것만 있습니다.

<strong>범위는 시작할 칸을 정할 뿐, 끝낼 칸을 정하지 않습니다.</strong>

다섯 번째 줄에서 처음으로 값이 무너집니다. `없으면 없다고만 답해. 더 찾지 마.` 가 붙는
자리이고, **범위를 하나도 안 짚었는데도** 17호출이 5호출이 됩니다. 올라가는 일 자체가
없어지기 때문입니다.

그리고 둘을 같이 적으면 여기서 다시 떨어집니다. 범위가 **시작할 칸**을, 중단 조건이
**끝낼 칸**을 정하니 탐색이 한 칸에서 끝납니다 — 1호출, $0.013. 아무것도 안 적었을 때의
**61분의 1**입니다.

<strong>둘은 짝일 때만 값을 합니다.</strong> 범위만 적으면 손해이고, 중단 조건만 적으면
4배 남짓이며, 같이 적어야 예순 배가 됩니다.

`무엇을 돌려줄지` 는 나머지 셋에 비하면 작습니다. 탐색이 한 칸에서 끝난 뒤에는 줄일
결과 자체가 얼마 없기 때문입니다. 다만 탐색이 길어질수록 쌓이는 쪽을 줄이므로, 위 줄에서
아래로 못 내려가는 상황일수록 값을 합니다.

<p><small>탐색어를 적으면 칸마다의 호출이 절반이 된다는 배수는 이 모형이 고른 예시 값입니다. 방향만 근거가 있고, 실제로 얼마나 주는지는 공개된 수가 없습니다.</small></p>
