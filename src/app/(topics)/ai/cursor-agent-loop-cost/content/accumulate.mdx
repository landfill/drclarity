export const title = '8만이 어떻게 304만이 되나';

사다리를 다 올라가도 **탐색이 문맥에 남긴 것은 83,600** 입니다. 그런데 같은 요청의
`Cache Read` 는 **3,040,100** — 남긴 것의 **36배**입니다.

곱해 준 것은 호출 수입니다. 호출마다 그때까지의 문맥이 **통째로 다시 실립니다.**

탐색과 무관한 몫부터 그렇습니다. 대화와 시스템 지시를 합친 시작 문맥 40,000 이 33번이면
그것만으로 **132만** — 이 행 캐시 읽기의 43% 입니다. 나머지가 탐색이 쌓은 것입니다.

<Link href="/ai/cursor-context-cost">앞 편</Link>의 어림이 그대로 들어맞습니다.

<strong>Total = 호출당 평균 문맥 × 호출 수 + 출력 총합</strong>

`Total 3,327,100` 에서 출력 42,900 을 빼고 33 으로 나누면 호출당 약 **99,500** 입니다.

**창은 넘지 않았습니다.** 가장 큰 호출이 20만의 82% 입니다. 이 요청이 비쌌던 것은
한도를 넘어서가 아니라, **한도 안쪽을 서른세 번 다시 실었기** 때문입니다.
