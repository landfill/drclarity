export const title = '9만이 어떻게 395만이 되나';

사다리를 다 올라가도 **탐색이 문맥에 남긴 것은 92,600** 입니다. 창 20만의 절반이 안 됩니다.
그런데 같은 요청의 `Cache Read` 는 **3,947,500** 입니다. 남긴 것의 **42.6배**입니다.

곱해 준 것은 호출 수입니다. 호출마다 그때까지의 문맥이 **통째로 다시 실립니다.**
한 번 쌓인 탐색 결과는 그 뒤의 모든 호출에 다시 들어갑니다.

탐색과 무관한 몫부터 그렇습니다. 대화와 시스템 지시를 합친 시작 문맥 40,000 은 호출마다
그대로 실리므로, 40번이면 그것만으로 **160만** — 이 행 캐시 읽기의 41% 입니다.
나머지가 탐색이 쌓은 것이 반복해 실린 몫입니다.

<Link href="/ai/cursor-context-cost">앞 편</Link>이 세운 어림이 여기에 그대로 들어맞습니다.

<strong>Total = 호출당 평균 문맥 × 호출 수 + 출력 총합</strong>

이 요청은 **105,695 × 40 + 52,000 = 4,279,800** 입니다. 앞 편은 왼쪽 항을 상수로 두었지만,
탐색하는 동안에는 자랍니다 — 첫 호출은 42,000 을 보고 가장 큰 호출은 182,300 을 봅니다.

**그래도 창은 넘지 않았습니다.** 가장 큰 호출이 창 20만의 91% 입니다. 이 요청이 비쌌던
것은 한도를 넘어서가 아니라, 한도 안쪽을 마흔 번 다시 실었기 때문입니다.
