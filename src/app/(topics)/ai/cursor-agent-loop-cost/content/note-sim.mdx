위의 표와 아래 화면은 <strong>이 페이지의 모형이 만들어 낸 값</strong>입니다. 실제 사용
기록을 읽거나 Cursor 에 요청을 보내지 않습니다. 그래서 네 항목의 합이 `Total` 과 어긋날
수 없고, 표와 화면이 같은 규칙 위에 있습니다.

모형과 예시 단가는 <Link href="/ai/cursor-context-cost">앞 편</Link>의 것을 그대로 씁니다.
`Cache Write` 가 0 이 아닌 것도 그 때문입니다 — 이 화면은 네 항목을 **모두 청구하는
모델**을 가정합니다. Cursor 자체 모델처럼 캐시 쓰기 칸이 `-` 인 모델에서는 그 칸이 0 으로
남습니다.
