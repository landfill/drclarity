호출마다 그때까지의 문맥이 통째로 다시 실립니다. 그 대부분이 이미 캐시에 있어
`Cache Read` 로 찍힙니다. **왼쪽 문맥 × 호출 수 — 오른쪽 숫자는 대체로 그 곱입니다.**

155.2K 에 호출 넷이면 63만쯤. 실제 자료의 `Cache Read 647,221` 이 그 언저리입니다.
같은 세션의 기록이 아니니 값이 맞아떨어질 이유는 없습니다. 맞는 것은 자릿수입니다.

호출마다 붙는 도구 결과의 양은 공개돼 있지 않아 예시 값으로 뒀습니다.
