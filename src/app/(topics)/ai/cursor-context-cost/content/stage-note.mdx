두 화면을 잇는 산수는 한 줄입니다. **호출마다 그때까지의 문맥이 통째로 다시 실리고,
그 대부분이 이미 캐시에 있어 `Cache Read` 로 찍힙니다.** 문맥 155.2K 짜리 채팅에서
요청 하나가 호출 넷을 만들면 캐시 읽기가 63만쯤 됩니다.

실제 자료의 `Cache Read 647,221` 이 그 언저리입니다. 같은 세션의 기록이 아니므로 값이
맞아떨어질 이유는 없습니다 — 맞는 것은 **자릿수가 어디서 오는지**입니다.

시뮬레이션은 캐시 경계를 **한 걸음 뒤**에 둡니다. 방금 붙은 재료는 경계 뒤에 있어
캐시에 올리지 못하고, 다음 호출에서 경계 앞으로 넘어가며 올라가고, 그 다음부터
재사용됩니다. 실제 Cursor 가 경계를 어디에 두는지, 호출마다 얼마가 붙는지는 공개돼
있지 않으므로 이 화면은 관계를 보이기 위한 모형으로 읽어 주세요.
