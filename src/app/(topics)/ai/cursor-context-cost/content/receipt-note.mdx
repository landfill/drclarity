예시 비용은 <strong>입력 100만 토큰을 1.00</strong>으로 둔 상대값입니다 (캐시 쓰기 1.25 ·
출력 5.00). 실제 요율이 아니라 **항목마다 단가가 다르다**는 것을 보기 위한 값입니다.

세 입력 항목은 서로 겹치지 않습니다. 캐시 경계보다 앞이면서 이미 캐시에 있으면 캐시 읽기,
앞이면서 이번에 올리면 캐시 쓰기, 뒤에 있으면 새 입력입니다. 그래서 셋을 더하면 그 호출의
입력 문맥이 됩니다.
