export const title = '20만 토큰 창에, 사용량은 60만?';

export const choices = [{"id": "same", "label": "15만 — 캐시에서 읽은 양도 그 안에 있다"}, {"id": "sum", "label": "30만 — 문맥과 캐시 읽기를 더한다"}, {"id": "none", "label": "0 — 캐시를 썼으니 입력은 없다"}];

입력 문맥 15만 토큰을 캐시에서 전부 가져왔습니다. **이번 한 번의 호출에 들어간 입력**은 얼마일까요?
