캐시에서 왔다고 모델이 안 보는 것은 아닙니다. 모델은 매번 **문맥 전체를 입력으로 받습니다** —
<Link href="/ai/context-limit">앞을 잊나</Link> 편에서 본 그대로입니다.

캐시가 바꾸는 것은 모델이 보는 양이 아니라 **그것을 다시 처리하는 비용**입니다.
그래서 `Cache Read 82.4K` 는 “82.4K 를 안 봤다” 가 아니라 “82.4K 를 다시 계산하지 않고
가져왔다” 는 뜻입니다.

아래에서 그 관계를 직접 움직여 봅니다.
