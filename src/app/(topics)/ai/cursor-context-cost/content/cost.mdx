export const title = '비용은 항목마다 따로 매겨진다';

이 표는 새 입력·캐시 쓰기·캐시 읽기·출력을 각각 다른 예시 단가로 계산합니다. **토큰이 많다고 금액도 같은 비율로 큰 것은 아닙니다.**

입력 전체를 캐시 읽기 + 캐시 쓰기 + 새 입력으로 나누는 구조는 <a href="https://platform.claude.com/docs/en/build-with-claude/prompt-caching" target="_blank" rel="noopener noreferrer">Anthropic 캐시 문서</a>를 따릅니다. 실제 항목과 단가는 모델·서비스마다 다릅니다.
