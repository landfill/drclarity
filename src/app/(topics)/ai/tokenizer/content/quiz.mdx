export const title = '같은 한 단어인데, 토큰 수도 같을까?';

export const choices = [{"id": "same", "label": "둘 다 한 단어니까 같다"}, {"id": "different", "label": "조각을 묶는 규칙에 따라 다르다"}];

`token`과 `2026`은 각각 띄어쓰기 없는 한 덩어리입니다. 이 토크나이저에서 토큰 수도 같을까요?
