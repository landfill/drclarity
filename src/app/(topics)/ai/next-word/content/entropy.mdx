export const title = '엔트로피가 말해주는 것';

엔트로피는 분포가 얼마나 퍼져 있는지를 **비트**로 잰 값입니다.
0 비트면 답이 하나로 정해진 상태, {props.maxBits} 비트면 후보
{props.candidateCount}개가 완전히 균등한 상태입니다.

`1 + 1 =` 문맥을 골라 보세요. temperature 를 한참 올려야 비로소
다른 답이 섞이기 시작합니다. 반대로 이야기 문맥은 낮은 temperature 에서도
여러 갈래가 살아 있습니다. **같은 설정이라도 문맥에 따라 흔들림의 폭이 다릅니다.**
