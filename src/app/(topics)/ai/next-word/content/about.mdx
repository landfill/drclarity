export const title = '이 화면에 대해';

여기 쓰인 후보와 점수는 **실제 모델을 호출해 얻은 값이 아니라** 설명을 위해
손으로 정한 예시입니다. temperature 가 확률을 바꾸는 방식은 실제와 같지만,
숫자 자체를 특정 모델의 출력으로 읽어서는 안 됩니다.

실제 모델은 후보가 수만 개입니다. 여기서는 상위 몇 개만 남겼습니다.
