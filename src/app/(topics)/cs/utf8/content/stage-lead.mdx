export const title = '글자 하나를 펼쳐 보기';

칸 하나에 그 조각이 실제로 차지하는 <strong>바이트</strong>가 들어 있습니다.

영어 알파벳은 하나, 한글은 셋, <strong>😀</strong> 같은 이모지는 넷을 씁니다.
화면에서는 똑같이 한 칸을 차지하는데 저장될 때 쓰는 자리는 이렇게 다릅니다.

바이트 수는 <strong>글자마다 정해져</strong> 있습니다. “이모지는 몇 바이트” 처럼
종류로 정해지는 것이 아니라 글자 하나하나가 자기 값을 갖습니다 —
`❤️` 나 `👨‍👩‍👧‍👦` 를 넣어 보면 칸이 여러 개로 나뉘는 것도 볼 수 있습니다.
